// config.json platform name
export const PLATFORM_NAME = 'tado';

// plugin name - must match the name as defined in package.json
export const PLUGIN_NAME = 'homebridge-platform-tado';

// plugin version
export const PLUGIN_VERSION = '1.0.2';
